FROM ubuntu:21.10

RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y openjdk-16-jdk ca-certificates-java && \
	update-ca-certificates -f && \
	apt-get clean

ENV JAVA_HOME /usr/lib/jvm/java-16-openjdk-amd64/
RUN export JAVA_HOME
RUN bash -c 'touch $JAVA_HOME/bin/javac'
